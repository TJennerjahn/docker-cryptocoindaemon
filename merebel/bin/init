#!/bin/bash

set -ex

if [ ! -e "$HOME/.Merebel/Merebel.conf" ]; then
    mkdir -p $HOME/.Merebel

    echo "Creating merebel.conf"

    # Seed a random password for JSON RPC server
    cat <<EOF > $HOME/.Merebel/Merebel.conf
testnet=${TESTNET:-0}
disablewallet=${DISABLEWALLET:-0}
printtoconsole=${PRINTTOCONSOLE:-1}
addressindex=${ADDRESSINDEX:-1}
txindex=${TXINDEX:-1}
server=${SERVER:-1}
rpcclienttimeout=${RPCCLIENTTIMEOUT:-30}
rpcport=${RPCPORT:-1344}
rpcuser=${RPCUSER:-merebelrpc}
rpcpassword=${RPCPASSWORD:-u7SV2ApjNmaiF5Y037h58CpWBGZLBDGULsYqzf6/nJdK}
EOF

fi

cat $HOME/.Merebel/Merebel.conf

echo "Initialization completed successfully"

#