version: '3'
services:
  blocknet:
    network_mode: "host"
    volumes:
      - 'blocknet-data:/blocknet'
    container_name: blocknet-node
    restart: always
    logging:
      options:
        max-size: 1g
    image: 'cryptocoindaemon:blocknet'
volumes:
  blocknet-data:
    external: false
