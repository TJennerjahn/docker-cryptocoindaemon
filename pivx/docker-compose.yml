version: '3'
services:
  polis:
    build: .
    network_mode: "host"
    volumes:
      - 'pivx-data:/pivx'
    container_name: pivx-node
    restart: always
    logging:
      options:
        max-size: 1g
    image: 'cryptocoindaemon:polis'
volumes:
  pivx-data:
    external: false
