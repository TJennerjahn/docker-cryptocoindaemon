version: '3'
services:
  syscoin:
    network_mode: "host"
    volumes:
      - 'syscoin-data:/syscoin'
    container_name: syscoin-node
    restart: always
    logging:
      options:
        max-size: 1g
    image: 'cryptocoindaemon:syscoin'
volumes:
  syscoin-data:
    external: false
