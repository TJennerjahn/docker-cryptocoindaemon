version: '3'
services:
  esbc:
    build: .
    network_mode: "host"
    volumes:
      - 'esbc-data:/esbc'
    container_name: esbc-node
    restart: always
    logging:
      options:
        max-size: 1g
    image: 'tjennerjahn/cryptocoindaemon:esbc'
volumes:
  esbc-data:
    external: false
