version: '3'
services:
  pivx:
    build: .
    volumes:
      - 'pivx-data:/pivx'
    container_name: pivx-node
    restart: always
    logging:
      options:
        max-size: 1g
    image: 'tjennerjahn/cryptocoindaemon:pivx'
volumes:
  pivx-data:
    external: false