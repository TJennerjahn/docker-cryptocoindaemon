version: '3'
services:
  xsn:
    build: .
    network_mode: "host"
    volumes:
      - 'xsn-data:/xsn'
    container_name: xsn-node
    restart: always
    logging:
      options:
        max-size: 1g
    image: 'tjennerjahn/cryptocoindaemon:xsn'
volumes:
  xsn-data:
    external: false
